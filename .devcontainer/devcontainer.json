{
    "name": "Research Environment",
    "dockerComposeFile": "docker-compose.yml",
    "service": "workspace",
    "workspaceFolder": "/home/vscode/vermeer/envs/ds_env",
    "forwardPorts": [
        3000,
        5173,
        5432,
        8080
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "njpwerner.autodocstring",
                "eamodio.gitlens",
                "github.vscode-github-actions",
                "MS-CEINTL.vscode-language-pack-ja"
            ],
            "settings": {
                "python.defaultInterpreterPath": "/opt/conda/envs/research/bin/python",
                "python.formatting.provider": "none",
                "python.analysis.typeCheckingMode": "basic",
                "python.linting.enabled": true,
                "python.linting.pylintEnabled": true
            }
        }
    },
    "remoteUser": "vscode",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y sudo && sudo usermod -aG sudo vscode && echo 'vscode ALL=(ALL) NOPASSWD:ALL' | sudo tee /etc/sudoers.d/vscode && sudo mkdir -p /home/vscode/vermeer/envs/ds_env && sudo chown -R vscode:vscode /home/vscode/vermeer && sudo chmod -R 755 /home/vscode/vermeer",
    "mounts": [
        "source=${localEnv:HOME}/.ssh,target=/home/vscode/.ssh,type=bind,consistency=cached",
        "source=${localEnv:HOME}/.gitconfig,target=/home/vscode/.gitconfig,type=bind,consistency=cached",
        "source=${localEnv:HOME}/.windsurf,target=/home/vscode/.windsurf,type=bind,consistency=cached"
    ],
    "runArgs": [
        "--init",
        "--security-opt",
        "seccomp=unconfined"
    ]
}
